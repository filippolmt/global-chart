# ExternalSecrets only - no deployments
deployments: {}
externalSecrets:
  app-config:
    secretkey: app-config
    remote:
      key: "apps/prod/config"
    secretstore:
      kind: ClusterSecretStore
      name: default-store
