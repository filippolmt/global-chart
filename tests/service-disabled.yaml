# Test deployment with service disabled
deployments:
  main:
    image:
      repository: busybox
      tag: latest
    service:
      enabled: false
      type: ClusterIP
      port: 8080
      targetPort: 8080
      protocol: TCP
