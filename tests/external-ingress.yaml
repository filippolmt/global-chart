# Ingress only (no deployments) - points to external service
deployments: {}
ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: backend.example.com
      service:
        name: backend-svc
        port: 9000
      paths:
        - path: /
          pathType: Prefix
