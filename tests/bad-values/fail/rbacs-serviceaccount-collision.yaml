# A role creating the ServiceAccount a deployment also creates: two manifests,
# one name, and the install dies on "already exists" (issue #122).
deployments:
  web:
    image: nginx:1.25
    serviceAccount: { name: shared }
rbacs:
  roles:
    - name: reader
      serviceAccount: { name: shared }
      rules: []
