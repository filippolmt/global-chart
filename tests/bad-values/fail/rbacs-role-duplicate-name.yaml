# Two entries with one name render two Roles under it; the second overwrites
# the first at apply (issue #122). Schema cannot express per-field uniqueness.
# Expected fail substring: "Name collision: Role 'reader' generated by rbacs.roles[1]"
rbacs:
  roles:
    - { name: reader, rules: [] }
    - { name: reader, rules: [] }
