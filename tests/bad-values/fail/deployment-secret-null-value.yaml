# A null Secret value rendered the base64 of the string "null", which the app
# read as its secret (issue #166). It fails like a null ConfigMap value.
# Expected fail substring: "deployments.web.secret.EMPTY: a null value reached a Secret"
deployments:
  web:
    image: nginx:1.25
    secret:
      EMPTY: null
