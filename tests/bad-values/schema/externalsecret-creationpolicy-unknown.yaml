# creationPolicy outside ESO's enum: it passed lint and the ExternalSecret CRD
# rejected it at apply.
externalSecrets:
  app:
    secretstore: { kind: ClusterSecretStore, name: store }
    secretkey: K
    remote: { key: k }
    target: { creationPolicy: owner }
