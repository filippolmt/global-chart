# The job scopes share the ServiceAccount definition with the deployment
# (issue #123): `Migrate_SA` is rejected here too.
deployments:
  web:
    image: nginx:1.25
    hooks:
      pre-install:
        migrate:
          command: ["true"]
          serviceAccount:
            create: true
            name: Migrate_SA
