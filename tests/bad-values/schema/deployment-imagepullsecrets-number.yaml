# A deployment's imagePullSecrets items share global.imagePullSecrets' shape,
# a string or {name}. A number used to pass the schema and crash the render
# with "wrong type for value" (issue #158).
deployments:
  web:
    image: nginx:1.25
    imagePullSecrets: [regcred, 5]
