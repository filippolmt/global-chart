# pathType is an enum on the API server: Exact, Prefix, ImplementationSpecific.
# A lowercase `prefix` passed lint and was rejected at apply (issue #156).
ingress:
  enabled: true
  hosts:
    - host: example.com
      service: { name: ext }
      paths:
        - path: /
          pathType: prefix
