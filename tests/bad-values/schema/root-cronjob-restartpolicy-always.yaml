# A Job pod takes restartPolicy OnFailure or Never; Always passed lint and was
# rejected by the API server (issue #158).
cronJobs:
  report:
    schedule: "0 3 * * *"
    image: busybox:1.36
    command: ["true"]
    restartPolicy: Always
