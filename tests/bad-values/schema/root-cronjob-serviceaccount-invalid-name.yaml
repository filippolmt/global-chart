# A root cronjob's own ServiceAccount is held to the same rule (issue #123).
cronJobs:
  cleanup:
    schedule: "0 0 * * *"
    image: busybox:1.36
    serviceAccount:
      create: true
      name: Cleanup_SA
