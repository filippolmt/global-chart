# A ServiceAccount name is a DNS-1123 subdomain in every scope (issue #123).
# `Web_SA` renders and passes `helm lint`, then the API server rejects it.
deployments:
  web:
    image: nginx:1.25
    serviceAccount:
      name: Web_SA
