# rbacs.roles[].serviceAccount.name becomes the ServiceAccount and the RoleBinding
# subject (issue #123). `Reader_SA` renders, lints, and is rejected at apply.
rbacs:
  roles:
    - name: reader
      serviceAccount:
        name: Reader_SA
      rules: []
