# An image digest with no algorithm prefix. The OCI grammar is
# <algorithm>:<encoded>, e.g. sha256:<hex>; the chart rendered nginx@abcdef
# and the failure surfaced only at pull time as InvalidImageName.
deployments:
  web:
    image:
      repository: nginx
      digest: "abcdef1234567890"
