# The key becomes the container name raw: a DNS-1123 label, so no uppercase.
deployments:
  app:
    image: nginx:1.25
    hooks:
      pre-install:
        Migrate:
          command: ["sh", "-c", "true"]
