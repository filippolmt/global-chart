# covers: kedaSecretTargetRef
# An unknown key in a secretTargetRef (typo of key): KEDA reads no key and the
# scaler authenticates with an empty credential.
kedaTriggerAuthentications:
  auth:
    secretTargetRef:
      - parameter: password
        name: redis
        keyy: password
