# dataFrom item with an unknown selector key (typo of "find") must be rejected
# by the schema before it reaches external-secrets at apply time.
externalSecrets:
  app:
    secretstore:
      kind: ClusterSecretStore
      name: my-store
    dataFrom:
      - fnid:
          name:
            regexp: "^APP_.*"
