# EnvVar.value is a string (issue #137), in a job's env. Root scope; env comes
# from jobCommon, so the deployment scope takes the same $ref.
hooks:
  pre-install:
    migrate:
      image: busybox:1.36
      command: ["true"]
      env:
        - name: LIMIT
          value: 10000000
