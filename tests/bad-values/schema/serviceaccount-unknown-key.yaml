# covers: serviceAccount
# An unknown key under a serviceAccount block (typo of automount). Without the
# constraint the token is mounted anyway and the intended restriction is absent.
deployments:
  web:
    image: nginx:1.25
    serviceAccount:
      create: true
      automont: false
