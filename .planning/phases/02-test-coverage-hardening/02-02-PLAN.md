---
phase: 02-test-coverage-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/global-chart/tests/hook_test.yaml
  - charts/global-chart/tests/deployment_test.yaml
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Every fail/required call in hook.yaml has a negative test asserting the exact error message"
    - "Hook deployment-level inheritance is tested across seven categories for key fields"
    - "Every fail/required call in deployment.yaml has a negative test asserting the exact error message"
    - "Deployment has seven-category tests for its own configurable fields"
  artifacts:
    - path: "charts/global-chart/tests/hook_test.yaml"
      provides: "Hook negative tests and seven-category inheritance coverage"
      contains: "Negative tests"
    - path: "charts/global-chart/tests/deployment_test.yaml"
      provides: "Deployment negative tests and seven-category field coverage"
      contains: "Negative tests"
  key_links:
    - from: "charts/global-chart/tests/hook_test.yaml"
      to: "charts/global-chart/templates/hook.yaml"
      via: "failedTemplate errorMessage assertions"
      pattern: "failedTemplate"
    - from: "charts/global-chart/tests/deployment_test.yaml"
      to: "charts/global-chart/templates/deployment.yaml"
      via: "failedTemplate errorMessage assertions"
      pattern: "failedTemplate"
---

<objective>
Add comprehensive negative tests and seven-category inheritance tests to hook_test.yaml, and negative tests plus seven-category field tests to deployment_test.yaml.

Purpose: Hook has 2 untested fail/required paths and lacks systematic seven-category coverage despite having 1514 lines of tests. Deployment has 2 untested fail paths. This plan closes the remaining gaps for the three inheritance-capable resource types.
Output: Updated hook_test.yaml and deployment_test.yaml with full negative and seven-category test coverage.
</objective>

<execution_context>
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage-hardening/02-CONTEXT.md
@.planning/phases/02-test-coverage-hardening/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hook negative tests and seven-category inheritance tests</name>
  <files>charts/global-chart/tests/hook_test.yaml</files>
  <read_first>
    - charts/global-chart/tests/hook_test.yaml
    - charts/global-chart/templates/hook.yaml
    - charts/global-chart/templates/_helpers.tpl
  </read_first>
  <action>
**Part A: Negative tests**

Add a section with `# ====== Negative tests: fail/required paths ======`.

The existing hook_test.yaml already has 1 negative test for `fromDeployment references non-existent deployment` (hook_test.yaml around line 52). Add the 2 missing negative tests:

1. **Root hook missing image (no image, no fromDeployment):**
   ```yaml
   - it: should fail when root hook has no image and no fromDeployment
     set:
       hooks:
         post-install:
           migrate:
             command: ["./migrate.sh"]
     asserts:
       - failedTemplate:
           errorMessage: "image is required for hooks.post-install.migrate (set hooks.post-install.migrate.image or hooks.post-install.migrate.fromDeployment)"
   ```

2. **Deployment-level hook missing image:**
   Use `template: templates/hook.yaml` if the test file does not already restrict to hook.yaml in the suite header. Set deployment with no image so the hook's `required` at line 201 triggers:
   ```yaml
   - it: should fail when deployment-level hook has no image and deployment has no image
     set:
       deployments:
         backend:
           hooks:
             pre-upgrade:
               migrate:
                 command: ["./migrate.sh"]
     asserts:
       - failedTemplate:
           errorMessage: "image is required for deployments.backend.hooks.pre-upgrade.migrate"
   ```
   Note: If the suite header targets `templates/hook.yaml` only, deployment.yaml won't render and the test works. If not, add `template: templates/hook.yaml`.

**Part B: Seven-category inheritance tests**

Add a section with `# ====== Seven-category inheritance tests (deployment-level Hooks) ======`.

The hook template uses the SAME `inheritedJobPodSpec` helper as CronJobs but with `includeDnsConfig: false` and `includeInitContainers: false`. Test the same 6 fields as Plan 01 but for hooks, to confirm the shared helper works for both callers:

For each field, 4 test cases (default, inheritance, override, explicit-empty).

1. **nodeSelector** -- path: `spec.template.spec.nodeSelector`
2. **tolerations** -- path: `spec.template.spec.tolerations`
3. **affinity** -- path: `spec.template.spec.affinity`
4. **podSecurityContext** -- path: `spec.template.spec.securityContext`
5. **securityContext** (container) -- path: `spec.template.spec.containers[0].securityContext`
6. **imagePullSecrets** -- path: `spec.template.spec.imagePullSecrets`

BEFORE writing each test, check the existing 1514-line hook_test.yaml for any test that already covers a seven-category case. The file already has many inheritance/override tests for hooks. For each existing test that maps to a seven-category case, add a comment like `# Seven-cat nodeSelector override: covered by existing test "should use hook nodeSelector"` and skip writing a duplicate.

Base values for deployment-level hook tests:
```yaml
set:
  deployments:
    backend:
      image: myapp:v1
      serviceAccount:
        create: true
      hooks:
        pre-upgrade:
          migrate:
            command: ["./migrate.sh"]
```

For hooks, documentIndex matters -- deployment-level hooks can emit SA (index 0) + Job (index 1), or just Job (index 0) when using deployment's SA. When deployment has `serviceAccount.create: true`, the hook inherits that SA and does NOT create its own, so the Job is at documentIndex 0 for the hook template.

**Important:** Hook uses `spec.template.spec` (not `spec.jobTemplate.spec.template.spec` like CronJob). The Job spec path is shorter.

Also note hooks do NOT inherit `dnsConfig` (`includeDnsConfig: false`), so skip dnsConfig seven-category for hooks.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hook_test.yaml contains `# ====== Negative tests: fail/required paths ======`
    - hook_test.yaml contains `errorMessage: "image is required for hooks.post-install.migrate`
    - hook_test.yaml contains `errorMessage: "image is required for deployments.backend.hooks.pre-upgrade.migrate"`
    - hook_test.yaml contains `# ====== Seven-category inheritance tests`
    - hook_test.yaml contains tests (or comments referencing existing tests) for nodeSelector, tolerations, affinity, podSecurityContext, securityContext, imagePullSecrets
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>All 3 Hook fail/required paths have negative tests (1 existing + 2 new). Seven-category inheritance is systematically covered for deployment-level hooks. `make unit-test` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Deployment negative tests and seven-category field tests</name>
  <files>charts/global-chart/tests/deployment_test.yaml</files>
  <read_first>
    - charts/global-chart/tests/deployment_test.yaml
    - charts/global-chart/templates/deployment.yaml
  </read_first>
  <action>
**Part A: Negative tests**

Add a section with `# ====== Negative tests: fail/required paths ======`.

Add 2 negative tests for deployment.yaml's fail/required calls:

1. **Image map with repository but no tag/digest (fail at line 87):**
   ```yaml
   - it: should fail when image has repository but no tag or digest
     set:
       deployments:
         main:
           image:
             repository: myorg/myapp
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.image requires either a tag or digest when repository is provided"
   ```

2. **No image at all (required at line 90):**
   ```yaml
   - it: should fail when deployment has no image defined
     set:
       deployments:
         main: {}
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.image must define a repository with tag/digest or be a full image reference"
   ```
   Note: `main: {}` means no image key at all. The `imageString` helper returns empty, triggering `required`.

**Part B: Seven-category field tests**

Deployment does NOT have inheritance (it's the top-level resource), but CONTEXT.md says "Apply the full seven-category pattern only to resources with inheritance logic: Deployment, CronJob, Hook." For Deployment, the relevant seven categories apply to its OWN configurable fields where behavior differs based on presence/absence:

Add a section with `# ====== Seven-category field tests (Deployment) ======`.

Test the following fields (4 categories each: default, set, explicit-zero/false/empty, failure):

1. **replicaCount** (hasKey pattern, default 2, falsy 0 must work)
   - default: not set -> replicas = 2
   - set: `replicaCount: 5` -> replicas = 5
   - explicit-zero: `replicaCount: 0` -> replicas = 0 (Phase 1 regression test may exist -- check and reference)
   - with-autoscaling: `autoscaling.enabled: true` -> replicas field absent

2. **imagePullSecrets** (deploy > global, hasKey chain)
   - default: not set, no global -> imagePullSecrets absent
   - set: `imagePullSecrets: [{name: "my-reg"}]` -> present
   - global-fallback: not set on deploy, `global.imagePullSecrets: [{name: "global-reg"}]` -> uses global
   - explicit-empty: `imagePullSecrets: []` -> absent (blocks global)

3. **service.enabled** (disabled = no Service)
   - This is tested in service_test.yaml. Add a comment referencing it rather than duplicating.

Check if existing deployment_test.yaml already has any of these tests. Phase 1 added `replicaCount: 0` regression test -- reference it with a comment if found.

Path references for assertions:
- replicas: `spec.replicas`
- imagePullSecrets: `spec.template.spec.imagePullSecrets`
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - deployment_test.yaml contains `# ====== Negative tests: fail/required paths ======`
    - deployment_test.yaml contains `errorMessage: "deployments.main.image requires either a tag or digest when repository is provided"`
    - deployment_test.yaml contains `errorMessage: "deployments.main.image must define a repository with tag/digest or be a full image reference"`
    - deployment_test.yaml contains `# ====== Seven-category field tests`
    - deployment_test.yaml contains tests for replicaCount and imagePullSecrets categories
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>All 2 Deployment fail/required paths have negative tests. Seven-category field coverage added for Deployment's hasKey-based fields. `make unit-test` passes.</done>
</task>

</tasks>

<verification>
All Hook and Deployment fail/required paths have negative tests. Seven-category coverage is systematic for both resource types.

```bash
cd /Users/filippomerante/project/github/global-chart && make unit-test && make lint-chart
```
</verification>

<success_criteria>
- 2 new Hook negative tests + existing 1 = all 3 Hook fail paths covered
- 2 new Deployment negative tests = all 2 Deployment fail paths covered
- 20+ seven-category tests across Hook (inheritance fields) and Deployment (own fields)
- `make unit-test` exits 0 with all tests passing
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage-hardening/02-02-SUMMARY.md`
</output>
