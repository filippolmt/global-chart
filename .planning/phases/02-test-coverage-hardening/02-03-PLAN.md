---
phase: 02-test-coverage-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/global-chart/tests/hpa_test.yaml
  - charts/global-chart/tests/ingress_test.yaml
  - charts/global-chart/tests/externalsecret_test.yaml
  - charts/global-chart/tests/mounted-configmap_test.yaml
  - charts/global-chart/tests/helpers_test.yaml
  - charts/global-chart/tests/cronjob_test.yaml
  - charts/global-chart/tests/deployment_test.yaml
  - charts/global-chart/tests/hook_test.yaml
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-04

must_haves:
  truths:
    - "Every fail/required call in HPA, Ingress, ExternalSecret, mounted-configmap, and helpers templates has a negative test"
    - "Boundary tests verify resource names at exactly 63 and 52 character limits and confirm truncation when exceeded"
    - "Edge cases for explicit empty lists, nil fields, and disabled deployment references are tested"
    - "Phase 1 regression tests are verified to exist for all bugs fixed in Phase 1"
  artifacts:
    - path: "charts/global-chart/tests/hpa_test.yaml"
      provides: "HPA negative tests for minReplicas/maxReplicas required"
      contains: "Negative tests"
    - path: "charts/global-chart/tests/ingress_test.yaml"
      provides: "Ingress negative tests for deployment reference validation"
      contains: "Negative tests"
    - path: "charts/global-chart/tests/externalsecret_test.yaml"
      provides: "ExternalSecret negative tests for all required fields"
      contains: "Negative tests"
    - path: "charts/global-chart/tests/mounted-configmap_test.yaml"
      provides: "Mounted ConfigMap negative tests for content required"
      contains: "Negative tests"
    - path: "charts/global-chart/tests/helpers_test.yaml"
      provides: "Helper negative tests for renderVolume and imagePullSecrets"
      contains: "Negative tests"
  key_links:
    - from: "charts/global-chart/tests/hpa_test.yaml"
      to: "charts/global-chart/templates/hpa.yaml"
      via: "failedTemplate errorMessage assertions"
      pattern: "failedTemplate"
    - from: "charts/global-chart/tests/externalsecret_test.yaml"
      to: "charts/global-chart/templates/externalsecret.yaml"
      via: "failedTemplate errorMessage assertions"
      pattern: "failedTemplate"
---

<objective>
Add negative tests for all remaining simpler resource templates (HPA, Ingress, ExternalSecret, mounted-configmap, helpers), boundary tests for name truncation limits, edge case tests, and verify Phase 1 regression test coverage.

Purpose: Closes the remaining 16 negative test gaps in simpler templates, adds systematic boundary tests for 63/52 char limits, and verifies TEST-01 (regression tests exist for all Phase 1 fixes).
Output: Updated test files with full negative coverage, boundary tests, and edge case tests.
</objective>

<execution_context>
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-coverage-hardening/02-CONTEXT.md
@.planning/phases/02-test-coverage-hardening/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add negative tests for HPA, Ingress, ExternalSecret, mounted-configmap, and helpers</name>
  <files>
    charts/global-chart/tests/hpa_test.yaml
    charts/global-chart/tests/ingress_test.yaml
    charts/global-chart/tests/externalsecret_test.yaml
    charts/global-chart/tests/mounted-configmap_test.yaml
    charts/global-chart/tests/helpers_test.yaml
  </files>
  <read_first>
    - charts/global-chart/tests/hpa_test.yaml
    - charts/global-chart/tests/ingress_test.yaml
    - charts/global-chart/tests/externalsecret_test.yaml
    - charts/global-chart/tests/mounted-configmap_test.yaml
    - charts/global-chart/tests/helpers_test.yaml
    - charts/global-chart/templates/hpa.yaml
    - charts/global-chart/templates/ingress.yaml
    - charts/global-chart/templates/externalsecret.yaml
    - charts/global-chart/templates/mounted-configmap.yaml
    - charts/global-chart/templates/_helpers.tpl
  </read_first>
  <action>
Add `# ====== Negative tests: fail/required paths ======` section to each file. Use `failedTemplate` with exact `errorMessage` for each test.

**HPA (hpa_test.yaml) -- 2 new tests:**

1. Missing minReplicas:
   ```yaml
   - it: should fail when HPA enabled without minReplicas
     set:
       deployments:
         main:
           image: nginx:1.25
           autoscaling:
             enabled: true
             maxReplicas: 10
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.autoscaling.minReplicas is required when HPA is enabled"
   ```

2. Missing maxReplicas:
   ```yaml
   - it: should fail when HPA enabled without maxReplicas
     set:
       deployments:
         main:
           image: nginx:1.25
           autoscaling:
             enabled: true
             minReplicas: 2
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.autoscaling.maxReplicas is required when HPA is enabled"
   ```

**Ingress (ingress_test.yaml) -- 2 new tests (1 already exists for disabled deployment):**

1. Non-existent deployment:
   ```yaml
   - it: should fail when ingress host references non-existent deployment
     set:
       deployments:
         frontend:
           image: nginx:1.25
       ingress:
         enabled: true
         hosts:
           - host: example.com
             deployment: nonexistent
             paths:
               - path: /
     asserts:
       - failedTemplate:
           errorMessage: "ingress host 'example.com' references deployment 'nonexistent' which does not exist"
   ```

2. Missing both deployment and service.name:
   ```yaml
   - it: should fail when ingress host has neither deployment nor service.name
     set:
       deployments:
         frontend:
           image: nginx:1.25
       ingress:
         enabled: true
         hosts:
           - host: example.com
             paths:
               - path: /
     asserts:
       - failedTemplate:
           errorMessage: "ingress host 'example.com' must specify either 'deployment' or 'service.name'"
   ```

**ExternalSecret (externalsecret_test.yaml) -- 5 new tests (1 exists for remote.key):**

Read the template to get exact `required` message strings. The tests need:

1. Missing `remote`:
   ```yaml
   - it: should fail when externalSecret has no remote
     set:
       externalSecrets:
         my-secret:
           secretstore:
             kind: ClusterSecretStore
             name: my-store
     asserts:
       - failedTemplate:
           errorMessage: "externalSecrets.my-secret.remote is mandatory"
   ```

2. Missing `secretstore`:
   ```yaml
   - it: should fail when externalSecret has no secretstore
     set:
       externalSecrets:
         my-secret:
           remote:
             key: my-remote-key
     asserts:
       - failedTemplate:
           errorMessage: "externalSecrets.my-secret.secretstore is mandatory"
   ```

3. Missing `secretkey` (the `required` at line 22 uses just `"secretkey is mandatory"` -- check the exact string in the template):
   Read the template carefully for the exact error message. It may be `externalSecrets.%s.remote.secretkey is mandatory` or just `secretkey is mandatory`.
   ```yaml
   - it: should fail when externalSecret remote has no secretkey
     set:
       externalSecrets:
         my-secret:
           remote:
             key: my-remote-key
           secretstore:
             kind: ClusterSecretStore
             name: my-store
     asserts:
       - failedTemplate:
           errorMessage: "secretkey is mandatory"
   ```

4. Missing `secretstore.kind`:
   ```yaml
   - it: should fail when externalSecret secretstore has no kind
     set:
       externalSecrets:
         my-secret:
           remote:
             key: my-remote-key
             secretkey: my-secret-key
           secretstore:
             name: my-store
     asserts:
       - failedTemplate:
           errorMessage: "externalSecrets.my-secret.secretstore.kind is mandatory"
   ```

5. Missing `secretstore.name`:
   ```yaml
   - it: should fail when externalSecret secretstore has no name
     set:
       externalSecrets:
         my-secret:
           remote:
             key: my-remote-key
             secretkey: my-secret-key
           secretstore:
             kind: ClusterSecretStore
     asserts:
       - failedTemplate:
           errorMessage: "externalSecrets.my-secret.secretstore.name is mandatory"
   ```

**Mounted ConfigMap (mounted-configmap_test.yaml) -- 2 new tests:**

1. File without content:
   ```yaml
   - it: should fail when mounted config file has no content
     set:
       deployments:
         main:
           image: nginx:1.25
           mountedConfigFiles:
             files:
               - name: my-config
                 targetPath: /etc/config/my.conf
                 filename: my.conf
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.mountedConfigFiles.files[0] (my-config): 'content' is required"
   ```

2. Bundle file without content:
   ```yaml
   - it: should fail when mounted config bundle file has no content
     set:
       deployments:
         main:
           image: nginx:1.25
           mountedConfigFiles:
             bundles:
               - mountDir: /etc/config
                 files:
                   - name: my-config
                     relPath: sub/my.conf
     asserts:
       - failedTemplate:
           errorMessage: "deployments.main.mountedConfigFiles.bundles[0].files[0] (my-config): 'content' is required"
   ```

**Helpers (helpers_test.yaml) -- 2 new tests (1 exists for digest-without-repository):**

1. renderVolume with unknown legacy type:
   ```yaml
   - it: should fail on unknown legacy volume type
     set:
       deployments:
         main:
           image: nginx:1.25
           volumes:
             - name: my-vol
               type: unknownType
               path: /some/path
     asserts:
       - failedTemplate:
           errorMessage: "renderVolume: unknown legacy volume type 'unknownType' for volume 'my-vol'"
   ```
   Note: This test must render `templates/deployment.yaml` since that's where `renderVolume` is called. Check if helpers_test.yaml uses a specific template or deployment.yaml. If helpers_test.yaml only targets `_helpers.tpl`, this test may need to be in deployment_test.yaml instead, or use `templates/deployment.yaml` explicitly.

2. imagePullSecrets invalid format:
   ```yaml
   - it: should fail when imagePullSecrets has invalid format
     set:
       deployments:
         main:
           image: nginx:1.25
           imagePullSecrets:
             - 123
     asserts:
       - failedTemplate:
           errorMessage: "imagePullSecrets must be a list of strings or objects with a 'name' key."
   ```
   Same note about template targeting.

For both helpers tests: check if helpers_test.yaml directly invokes helpers via `template:` or uses a wrapper. If the existing test for "digest without repository" uses `templates/deployment.yaml`, follow the same pattern.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hpa_test.yaml contains `errorMessage: "deployments.main.autoscaling.minReplicas is required when HPA is enabled"`
    - hpa_test.yaml contains `errorMessage: "deployments.main.autoscaling.maxReplicas is required when HPA is enabled"`
    - ingress_test.yaml contains `errorMessage: "ingress host 'example.com' references deployment 'nonexistent' which does not exist"`
    - ingress_test.yaml contains `errorMessage: "ingress host 'example.com' must specify either 'deployment' or 'service.name'"`
    - externalsecret_test.yaml contains `errorMessage: "externalSecrets.my-secret.remote is mandatory"`
    - externalsecret_test.yaml contains `errorMessage: "externalSecrets.my-secret.secretstore is mandatory"`
    - externalsecret_test.yaml contains `errorMessage: "externalSecrets.my-secret.secretstore.kind is mandatory"`
    - externalsecret_test.yaml contains `errorMessage: "externalSecrets.my-secret.secretstore.name is mandatory"`
    - mounted-configmap_test.yaml contains `errorMessage:` with `'content' is required`
    - helpers_test.yaml (or deployment_test.yaml) contains `errorMessage:` with `renderVolume: unknown legacy volume type`
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>All 16 remaining negative test gaps are closed across HPA (2), Ingress (2), ExternalSecret (5), mounted-configmap (2), helpers (2). Combined with existing 7 + Plan 01 (3) + Plan 02 (4) = 30/30 fail/required paths covered. `make unit-test` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add boundary tests and edge case tests, verify Phase 1 regression coverage</name>
  <files>
    charts/global-chart/tests/cronjob_test.yaml
    charts/global-chart/tests/deployment_test.yaml
    charts/global-chart/tests/hook_test.yaml
  </files>
  <read_first>
    - charts/global-chart/tests/cronjob_test.yaml
    - charts/global-chart/tests/deployment_test.yaml
    - charts/global-chart/tests/hook_test.yaml
    - charts/global-chart/templates/_helpers.tpl
  </read_first>
  <action>
**Part A: Boundary tests (TEST-04)**

Add `# ====== Boundary tests: name truncation ======` sections.

**In deployment_test.yaml -- 63-char limit:**

Name pattern: `{release}-global-chart-{deploymentName}`. With release `myrel`: prefix = `myrel-global-chart-` = 19 chars. Max deployment name = 44 chars.

1. Exact 63 chars (passes intact):
   ```yaml
   - it: should preserve deployment name at exactly 63 chars
     release:
       name: myrel
     set:
       deployments:
         aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:  # 44 chars
           image: nginx:1.25
     asserts:
       - equal:
           path: metadata.name
           value: "myrel-global-chart-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
       - matchRegex:
           path: metadata.name
           pattern: "^.{63}$"
   ```

2. Over 63 chars (truncated):
   ```yaml
   - it: should truncate deployment name when exceeding 63 chars
     release:
       name: myrel
     set:
       deployments:
         aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:  # 52 chars, total = 71
           image: nginx:1.25
     asserts:
       - matchRegex:
           path: metadata.name
           pattern: "^.{1,63}$"
       - not: true
         matchRegex:
           path: metadata.name
           pattern: "-$"
   ```
   Verify the name does not end with a hyphen (trimSuffix "-" in the helper).

**In cronjob_test.yaml -- 52-char limit (root-level):**

Name pattern: `{release}-global-chart-{cronName}`. With release `myrel`: prefix = 19 chars. Max cronName = 33 chars.

1. Exact 52 chars:
   ```yaml
   - it: should preserve root cronjob name at exactly 52 chars
     release:
       name: myrel
     set:
       cronJobs:
         aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:  # 33 chars
           schedule: "0 2 * * *"
           image: busybox:latest
           command: ["echo"]
     asserts:
       - equal:
           path: metadata.name
           value: "myrel-global-chart-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
       - matchRegex:
           path: metadata.name
           pattern: "^.{52}$"
   ```

2. Over 52 chars:
   ```yaml
   - it: should truncate root cronjob name when exceeding 52 chars
     release:
       name: myrel
     set:
       cronJobs:
         aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:  # 40 chars, total = 59
           schedule: "0 2 * * *"
           image: busybox:latest
           command: ["echo"]
     asserts:
       - matchRegex:
           path: metadata.name
           pattern: "^.{1,52}$"
   ```

**In hook_test.yaml -- 63-char limit:**

Name pattern: `{release}-global-chart-{hookType}-{jobName}`. With release `myrel` + `post-install`: prefix = `myrel-global-chart-post-install-` = 32 chars. Max jobName = 31 chars.

1. Exact 63 chars:
   ```yaml
   - it: should preserve hook name at exactly 63 chars
     release:
       name: myrel
     set:
       hooks:
         post-install:
           aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:  # 31 chars
             image: busybox:latest
             command: ["echo"]
     asserts:
       - equal:
           path: metadata.name
           value: "myrel-global-chart-post-install-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
         documentIndex: 1
       - matchRegex:
           path: metadata.name
           pattern: "^.{63}$"
         documentIndex: 1
   ```
   Note: Root hooks emit SA at index 0, Job at index 1. Verify this by checking existing test patterns. If the hook does not create SA (has explicit `serviceAccountName`), Job is at index 1 (SA still created by default for root hooks).

2. Over 63 chars (truncated):
   Same pattern, 40-char job name.

**Part B: Edge cases (from CONTEXT.md)**

Add `# ====== Edge cases ======` sections in the relevant test files.

1. **Explicit empty lists** in deployment_test.yaml:
   ```yaml
   - it: "edge case: imagePullSecrets empty list blocks global fallback"
     set:
       global:
         imagePullSecrets:
           - name: global-secret
       deployments:
         main:
           image: nginx:1.25
           imagePullSecrets: []
     asserts:
       - isNull:
           path: spec.template.spec.imagePullSecrets
   ```

   ```yaml
   - it: "edge case: tolerations empty list produces no output"
     set:
       deployments:
         main:
           image: nginx:1.25
           tolerations: []
     asserts:
       - isNull:
           path: spec.template.spec.tolerations
   ```

   ```yaml
   - it: "edge case: nodeSelector empty map produces no output"
     set:
       deployments:
         main:
           image: nginx:1.25
           nodeSelector: {}
     asserts:
       - isNull:
           path: spec.template.spec.nodeSelector
   ```

2. **Disabled deployment references** in cronjob_test.yaml (if not already covered):
   ```yaml
   - it: "edge case: deployment-level cronjobs not rendered when deployment disabled"
     set:
       deployments:
         backend:
           enabled: false
           image: myapp:v1
           cronJobs:
             cleanup:
               schedule: "0 4 * * *"
               command: ["./cleanup.sh"]
     asserts:
       - hasDocuments:
           count: 0
   ```

**Part C: Verify Phase 1 regression test coverage (TEST-01)**

Do NOT write new tests. Instead, verify that all 13 Phase 1 regression tests documented in the RESEARCH exist. Check by grep for the specific test names or errorMessage strings. The 13 tests are:
- cronjob_test: automountServiceAccountToken false, successfulJobsHistoryLimit 0, failedJobsHistoryLimit 0, historyLimit 0 deployment-level, SA inheritance existing SA
- hook_test: hook-weight 0 (4 tests: root SA, root Job, deploy SA, deploy Job)
- deployment_test: replicaCount 0
- mounted-configmap_test: truncation boundary (files), truncation boundary (bundles)
- externalsecret_test: truncation boundary

If any are missing, add them. If all exist, add a comment at the top of the relevant test sections: `# Phase 1 regression tests verified present (TEST-01): see tests above`.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - deployment_test.yaml contains `# ====== Boundary tests: name truncation ======`
    - deployment_test.yaml contains a test with `pattern: "^.{63}$"`
    - cronjob_test.yaml contains a test with `pattern: "^.{52}$"`
    - hook_test.yaml contains a boundary test for 63-char hook name
    - deployment_test.yaml contains `edge case: imagePullSecrets empty list blocks global fallback`
    - deployment_test.yaml contains `edge case: nodeSelector empty map`
    - cronjob_test.yaml contains `edge case: deployment-level cronjobs not rendered when deployment disabled` (or existing equivalent)
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>Boundary tests cover exact-limit and over-limit for 63-char (Deployment, Hook) and 52-char (CronJob) name limits. Edge cases verify explicit empty lists block inheritance/output. Phase 1 regression tests verified present. `make unit-test` passes.</done>
</task>

</tasks>

<verification>
All remaining negative test gaps closed. Boundary tests systematic. Edge cases covered. Phase 1 regressions verified.

```bash
cd /Users/filippomerante/project/github/global-chart && make unit-test && make lint-chart
```
</verification>

<success_criteria>
- 16 new negative tests across HPA (2), Ingress (2), ExternalSecret (5), mounted-configmap (2), helpers (2), plus 3 edge case tests
- Boundary tests: 2 for 63-char (Deployment), 2 for 52-char (CronJob), 2 for 63-char (Hook)
- Phase 1 regression coverage (13 tests) verified present
- `make unit-test` exits 0 with all tests passing
- Total: 30/30 fail/required paths covered (7 pre-existing + 23 new across all 3 plans)
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage-hardening/02-03-SUMMARY.md`
</output>
