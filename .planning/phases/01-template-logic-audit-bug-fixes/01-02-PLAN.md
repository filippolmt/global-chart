---
phase: 01-template-logic-audit-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - charts/global-chart/templates/cronjob.yaml
  - charts/global-chart/templates/hook.yaml
  - charts/global-chart/templates/deployment.yaml
  - charts/global-chart/templates/mounted-configmap.yaml
  - charts/global-chart/templates/externalsecret.yaml
  - charts/global-chart/tests/cronjob_test.yaml
  - charts/global-chart/tests/hook_test.yaml
  - charts/global-chart/tests/deployment_test.yaml
  - charts/global-chart/tests/externalsecret_test.yaml
  - charts/global-chart/tests/mounted-configmap_test.yaml
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "Setting `automountServiceAccountToken: false` on a root-level CronJob produces `automountServiceAccountToken: false` in the SA manifest"
    - "Setting `successfulJobsHistoryLimit: 0` on a CronJob produces `successfulJobsHistoryLimit: 0` in the manifest, not `2`"
    - "Setting `weight: '0'` on a Hook produces `helm.sh/hook-weight: '0'` in annotations, not '5' or '10'"
    - "A CronJob inside a deployment with `serviceAccount: {create: false, name: existing-sa}` renders `serviceAccountName: existing-sa`"
    - "A mounted configmap name exceeding 63 chars is truncated to 63 chars with trimSuffix"
    - "An ExternalSecret name exceeding 63 chars is truncated to 63 chars with trimSuffix"
  artifacts:
    - path: "charts/global-chart/templates/cronjob.yaml"
      provides: "Fixed falsy-value masking and SA inheritance"
      contains: "hasKey $job \"automountServiceAccountToken\""
    - path: "charts/global-chart/templates/hook.yaml"
      provides: "Fixed falsy-value masking on hook weight"
      contains: "hasKey $command \"weight\""
    - path: "charts/global-chart/templates/mounted-configmap.yaml"
      provides: "Truncation guard on configmap names"
      contains: "trunc 63"
    - path: "charts/global-chart/templates/externalsecret.yaml"
      provides: "Truncation guard on ExternalSecret names"
      contains: "trunc 63"
  key_links:
    - from: "charts/global-chart/tests/cronjob_test.yaml"
      to: "charts/global-chart/templates/cronjob.yaml"
      via: "regression tests for falsy-value and SA inheritance fixes"
      pattern: "automountServiceAccountToken.*false"
    - from: "charts/global-chart/tests/hook_test.yaml"
      to: "charts/global-chart/templates/hook.yaml"
      via: "regression test for hook weight 0"
      pattern: "hook-weight.*0"
---

<objective>
Apply all targeted template fixes identified in the AUDIT-REPORT: fix falsy-value masking in `default` calls (TMPL-01), fix CronJob SA inheritance to match Hooks (TMPL-02), and add missing truncation guards (TMPL-03). Each fix includes a regression test.

Purpose: These are direct correctness fixes -- users who pass valid values like `false`, `0`, or `""` should not have them silently replaced. CronJobs should inherit SA identically to Hooks. Resource names must never exceed Kubernetes limits.
Output: Fixed template files + regression tests passing via `make unit-test`
</objective>

<execution_context>
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-template-logic-audit-bug-fixes/01-CONTEXT.md
@.planning/phases/01-template-logic-audit-bug-fixes/01-RESEARCH.md
@.planning/phases/01-template-logic-audit-bug-fixes/AUDIT-REPORT.md
@.planning/phases/01-template-logic-audit-bug-fixes/01-01-SUMMARY.md

<interfaces>
<!-- Established patterns from existing codebase that executor MUST follow -->

hasKey + ternary pattern for boolean defaults:
```yaml
{{- $autoMount := true -}}
{{- if hasKey $job "automountServiceAccountToken" -}}
  {{- $autoMount = $job.automountServiceAccountToken -}}
{{- end -}}
automountServiceAccountToken: {{ $autoMount }}
```

hasKey + if/else for integer defaults:
```yaml
{{- if hasKey $job "successfulJobsHistoryLimit" }}
successfulJobsHistoryLimit: {{ $job.successfulJobsHistoryLimit }}
{{- else }}
successfulJobsHistoryLimit: 2
{{- end }}
```

Truncation pattern:
```yaml
name: {{ printf "%s-md-cm-%s" $depFullname $f.name | trunc 63 | trimSuffix "-" }}
```

SA inheritance reference (hook.yaml lines 206-212):
```yaml
{{- $deploymentSAName := "" -}}
{{- if $deploySA.create -}}
  {{- $deploymentSAName = include "global-chart.deploymentServiceAccountName" (dict "root" $root "deploymentName" $deployName "deployment" $deploy) -}}
{{- else if $deploySA.name -}}
  {{- $deploymentSAName = $deploySA.name -}}
{{- end -}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix falsy-value masking and CronJob SA inheritance in templates</name>
  <files>
    charts/global-chart/templates/cronjob.yaml
    charts/global-chart/templates/hook.yaml
    charts/global-chart/templates/deployment.yaml
    charts/global-chart/tests/cronjob_test.yaml
    charts/global-chart/tests/hook_test.yaml
    charts/global-chart/tests/deployment_test.yaml
  </files>
  <read_first>
    charts/global-chart/templates/cronjob.yaml
    charts/global-chart/templates/hook.yaml
    charts/global-chart/templates/deployment.yaml
    charts/global-chart/tests/cronjob_test.yaml
    charts/global-chart/tests/hook_test.yaml
    charts/global-chart/tests/deployment_test.yaml
    .planning/phases/01-template-logic-audit-bug-fixes/AUDIT-REPORT.md
  </read_first>
  <behavior>
    - Test: root CronJob with `automountServiceAccountToken: false` renders SA with `automountServiceAccountToken: false` (not `true`)
    - Test: root CronJob with `successfulJobsHistoryLimit: 0` renders `successfulJobsHistoryLimit: 0` (not `2`)
    - Test: root CronJob with `failedJobsHistoryLimit: 0` renders `failedJobsHistoryLimit: 0` (not `2`)
    - Test: deployment-level CronJob with same falsy values behaves identically
    - Test: root Hook SA annotation with `weight: "0"` renders `helm.sh/hook-weight: "0"` (not `"5"`)
    - Test: root Hook Job annotation with `weight: "0"` renders `helm.sh/hook-weight: "0"` (not `"10"`)
    - Test: deployment-level Hook with `weight: "0"` behaves identically
    - Test: deployment-level CronJob inherits SA when deployment has `serviceAccount: {create: false, name: "existing-sa"}` -- renders `serviceAccountName: existing-sa`
    - Test: deployment with `replicaCount: 0` (HPA disabled) renders `replicas: 0` (not `2`) -- use hasKey pattern
  </behavior>
  <action>
**Write regression tests FIRST (RED phase), then apply fixes (GREEN phase).**

**Fixes to apply in cronjob.yaml:**

1. **Line 47** (root-level SA automountServiceAccountToken): Replace `{{ default true $job.automountServiceAccountToken }}` with:
```yaml
{{- $saAutomount := true -}}
{{- if hasKey $job "automountServiceAccountToken" -}}
  {{- $saAutomount = $job.automountServiceAccountToken -}}
{{- end -}}
automountServiceAccountToken: {{ $saAutomount }}
```

2. **Lines 62-64** (root-level CronJob spec): Replace the three `default` lines with hasKey patterns:
```yaml
  concurrencyPolicy: {{ default "Forbid" $job.concurrencyPolicy | quote }}
{{- if hasKey $job "successfulJobsHistoryLimit" }}
  successfulJobsHistoryLimit: {{ $job.successfulJobsHistoryLimit }}
{{- else }}
  successfulJobsHistoryLimit: 2
{{- end }}
{{- if hasKey $job "failedJobsHistoryLimit" }}
  failedJobsHistoryLimit: {{ $job.failedJobsHistoryLimit }}
{{- else }}
  failedJobsHistoryLimit: 2
{{- end }}
```
Note: `concurrencyPolicy` keeps `default "Forbid"` because empty string is not valid K8s.

3. **Lines 182-186** (deployment-level SA inheritance): Add `else if $deploySA.name` branch after the `if $deploySA.create` block:
```yaml
{{- $deploymentSAName := "" -}}
{{- if $deploySA.create -}}
  {{- $deploymentSAName = include "global-chart.deploymentServiceAccountName" (dict "root" $root "deploymentName" $deployName "deployment" $deploy) -}}
{{- else if $deploySA.name -}}
  {{- /* Deployment references an existing SA - inherit it */ -}}
  {{- $deploymentSAName = $deploySA.name -}}
{{- end -}}
```

4. **Lines 257-259** (deployment-level CronJob spec): Apply the same hasKey pattern for successfulJobsHistoryLimit and failedJobsHistoryLimit as in fix #2.

**Fixes to apply in hook.yaml:**

5. **Line 65** (root-level SA annotation): Replace `default "5" $command.weight` with hasKey pattern:
```yaml
{{- if hasKey $command "weight" -}}
    "helm.sh/hook-weight": {{ $command.weight | quote }}
{{- else -}}
    "helm.sh/hook-weight": "5"
{{- end }}
```

6. **Line 81** (root-level Job annotation): Same pattern but default "10":
```yaml
{{- if hasKey $command "weight" -}}
    "helm.sh/hook-weight": {{ $command.weight | quote }}
{{- else -}}
    "helm.sh/hook-weight": "10"
{{- end }}
```

7. **Lines 265, 286** (deployment-level hook): Apply same hasKey pattern for weight with defaults "5" and "10".

**Fixes to apply in deployment.yaml:**

8. **Line 20**: Replace `$deploy.replicaCount | default 2` with:
```yaml
{{- if hasKey $deploy "replicaCount" }}
  replicas: {{ $deploy.replicaCount }}
{{- else }}
  replicas: 2
{{- end }}
```

After all fixes, run `make lint-chart && make unit-test` to confirm all existing tests still pass AND new regression tests pass.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make lint-chart && make unit-test</automated>
  </verify>
  <acceptance_criteria>
    - cronjob.yaml contains `hasKey $job "automountServiceAccountToken"` (no more `default true $job.automountServiceAccountToken`)
    - cronjob.yaml contains `hasKey $job "successfulJobsHistoryLimit"` (no more `default 2 $job.successfulJobsHistoryLimit`)
    - cronjob.yaml contains `else if $deploySA.name` in the deployment-level SA resolution block
    - hook.yaml contains `hasKey $command "weight"` (no more `default "5" $command.weight` or `default "10" $command.weight`)
    - deployment.yaml contains `hasKey $deploy "replicaCount"` (no more `$deploy.replicaCount | default 2`)
    - cronjob_test.yaml contains a test asserting `automountServiceAccountToken: false` when set to false
    - cronjob_test.yaml contains a test asserting `successfulJobsHistoryLimit: 0` when set to 0
    - cronjob_test.yaml contains a test asserting SA inheritance from `serviceAccount.create: false` with `name`
    - hook_test.yaml contains a test asserting `hook-weight: "0"` when weight is "0"
    - `make unit-test` exits 0
    - `make lint-chart` exits 0
  </acceptance_criteria>
  <done>All falsy-value masking bugs are fixed with hasKey patterns, CronJob SA inheritance matches Hook SA inheritance, and regression tests verify each fix</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add truncation guards to mounted-configmap and externalsecret names</name>
  <files>
    charts/global-chart/templates/mounted-configmap.yaml
    charts/global-chart/templates/externalsecret.yaml
    charts/global-chart/templates/deployment.yaml
    charts/global-chart/tests/mounted-configmap_test.yaml
    charts/global-chart/tests/externalsecret_test.yaml
    charts/global-chart/tests/deployment_test.yaml
  </files>
  <read_first>
    charts/global-chart/templates/mounted-configmap.yaml
    charts/global-chart/templates/externalsecret.yaml
    charts/global-chart/templates/deployment.yaml
    charts/global-chart/tests/mounted-configmap_test.yaml
    charts/global-chart/tests/externalsecret_test.yaml
    charts/global-chart/tests/deployment_test.yaml
    .planning/phases/01-template-logic-audit-bug-fixes/AUDIT-REPORT.md
  </read_first>
  <behavior>
    - Test: mounted configmap with a deployment name that makes the full name exceed 63 chars produces a name exactly 63 chars (truncated)
    - Test: mounted configmap volume reference in deployment.yaml uses the same truncated name as the configmap resource itself
    - Test: ExternalSecret with a name that makes the full name exceed 63 chars produces a name exactly at or under 63 chars
    - Test: mounted configmap bundle volume reference also uses the truncated name
  </behavior>
  <action>
**Write regression tests FIRST (RED phase), then apply fixes (GREEN phase).**

**Fix mounted-configmap.yaml:**

1. **Line 16** (files mode): Replace:
```yaml
  name: {{ $depFullname }}-md-cm-{{ $f.name }}
```
with:
```yaml
  name: {{ printf "%s-md-cm-%s" $depFullname $f.name | trunc 63 | trimSuffix "-" }}
```

2. Also check if there is a second naming pattern for bundles (lines 26+). The bundle configmap names use the same `$depFullname -md-cm-{{ $f.name }}` pattern in the range loop. Apply the same `printf ... | trunc 63 | trimSuffix "-"` fix to bundle configmap names.

**Fix deployment.yaml volume references:**

3. **Line 174** (files volume reference): Replace:
```yaml
            name: {{ $depFullname }}-md-cm-{{ $f.name }}
```
with:
```yaml
            name: {{ printf "%s-md-cm-%s" $depFullname $f.name | trunc 63 | trimSuffix "-" }}
```

4. **Line 184** (bundles projected source reference): Apply the same truncation pattern to match the configmap name.

**CRITICAL:** The volume reference name in deployment.yaml MUST match the configmap resource name in mounted-configmap.yaml exactly. Both must use the identical `printf` + `trunc 63` pattern.

**Fix externalsecret.yaml:**

5. **Line 7**: Replace:
```yaml
{{- $secretName := printf "%s-%s" (include "global-chart.fullname" $root) $name -}}
```
with:
```yaml
{{- $secretName := printf "%s-%s" (include "global-chart.fullname" $root) $name | trunc 63 | trimSuffix "-" -}}
```

After all fixes, run `make lint-chart && make unit-test`.

**Test approach for truncation boundary tests:**
Use values with long deployment names (e.g., 50+ character names) that would push the combined name over 63 chars. Assert that the rendered name has at most 63 characters and does not end with `-`.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make lint-chart && make unit-test</automated>
  </verify>
  <acceptance_criteria>
    - mounted-configmap.yaml contains `trunc 63` on every `name:` line
    - externalsecret.yaml contains `trunc 63` on the `$secretName` assignment
    - deployment.yaml volume references for mounted configmaps contain `trunc 63`
    - mounted-configmap_test.yaml contains a test case with a long name verifying truncation at 63 chars
    - externalsecret_test.yaml contains a test case with a long name verifying truncation at 63 chars
    - `make unit-test` exits 0
    - `make lint-chart` exits 0
  </acceptance_criteria>
  <done>All resource names have truncation guards at their appropriate limits (63 chars for configmaps and externalsecrets), volume references match their configmap names, and boundary tests verify correct truncation</done>
</task>

</tasks>

<verification>
- `make lint-chart` passes with zero errors across all test scenarios
- `make unit-test` passes with all existing tests plus new regression tests
- No `default true` or `default 2` patterns remain on user-facing fields identified in the audit
- CronJob SA inheritance path includes `else if $deploySA.name`
- All resource name constructions have `trunc N | trimSuffix "-"`
</verification>

<success_criteria>
- All TMPL-01 falsy-value fixes applied with regression tests
- TMPL-02 CronJob SA inheritance fixed with regression test
- TMPL-03 truncation guards added to mounted-configmap and externalsecret with boundary tests
- `make lint-chart && make unit-test` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-logic-audit-bug-fixes/01-02-SUMMARY.md`
</output>
