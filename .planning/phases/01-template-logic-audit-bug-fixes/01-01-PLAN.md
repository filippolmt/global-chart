---
phase: 01-template-logic-audit-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/global-chart/templates/cronjob.yaml
  - charts/global-chart/templates/hook.yaml
  - charts/global-chart/tests/cronjob_test.yaml
  - charts/global-chart/tests/hook_test.yaml
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-05

must_haves:
  truths:
    - "Setting successfulJobsHistoryLimit: 0 on a root-level CronJob renders 0, not 2"
    - "Setting failedJobsHistoryLimit: 0 on a deployment-level CronJob renders 0, not 2"
    - "Setting automountServiceAccountToken: false on a root-level CronJob SA renders false, not true"
    - "Hook prerequisite ConfigMap/Secret get weight 3, SA gets weight 5, Job gets weight 10"
    - "Setting any previously-masked field to a falsy value (0, false) renders that exact value in the manifest, not the default substitute"
    - "When a deployment has both pre-install and pre-upgrade hooks, prerequisite ConfigMap annotation contains both hook types"
  artifacts:
    - path: "charts/global-chart/templates/cronjob.yaml"
      provides: "Falsy-safe CronJob template"
      contains: "hasKey $job"
    - path: "charts/global-chart/templates/hook.yaml"
      provides: "Falsy-safe Hook template with correct prerequisite weights and aggregated hook types"
      contains: "\"helm.sh/hook-weight\": \"3\""
    - path: "charts/global-chart/tests/cronjob_test.yaml"
      provides: "Zero-value and false-value test cases"
      contains: "successfulJobsHistoryLimit: 0"
    - path: "charts/global-chart/tests/hook_test.yaml"
      provides: "Hook prerequisite weight and hook type aggregation test cases"
      contains: "hook-weight"
  key_links:
    - from: "charts/global-chart/templates/cronjob.yaml"
      to: "spec.successfulJobsHistoryLimit"
      via: "hasKey + ternary pattern"
      pattern: "ternary.*successfulJobsHistoryLimit.*2.*hasKey"
    - from: "charts/global-chart/templates/hook.yaml"
      to: "helm.sh/hook-weight annotations"
      via: "hardcoded weight values"
      pattern: "hook-weight.*\"3\""
    - from: "charts/global-chart/templates/hook.yaml"
      to: "helm.sh/hook annotation on prerequisite resources"
      via: "aggregated hook types from deployment"
      pattern: "helm.sh/hook.*join"
---

<objective>
Fix all falsy masking bugs in cronjob.yaml and hook.yaml, convert every `default` usage to `hasKey` + `ternary`, fix hook prerequisite ConfigMap/Secret weight ordering, and fix hook type aggregation on prerequisite resources.

Purpose: These two files contain the most critical falsy masking bugs (BUG-01, BUG-02) and the hook weight ordering issue (BUG-05). Fixing them first establishes the pattern for the remaining audit.
Output: Corrected cronjob.yaml and hook.yaml with full hasKey coverage and passing tests.
</objective>

<execution_context>
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/workflows/execute-plan.md
@/Users/filippomerante/project/github/global-chart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-template-logic-audit-bug-fixes/01-CONTEXT.md
@.planning/phases/01-template-logic-audit-bug-fixes/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Existing hasKey + ternary pattern (already correct in deployment-level cronjob section) -->
From charts/global-chart/templates/cronjob.yaml (lines 210-211, CORRECT reference pattern):
```yaml
{{- if hasKey (default (dict) $job.serviceAccount) "automountServiceAccountToken" -}}
  {{- $saAutomount = (default (dict) $job.serviceAccount).automountServiceAccountToken -}}
{{- end -}}
```

<!-- Single-line ternary pattern (canonical) -->
```yaml
successfulJobsHistoryLimit: {{ ternary $job.successfulJobsHistoryLimit 2 (hasKey $job "successfulJobsHistoryLimit") }}
```

<!-- Hook weight hierarchy per user decision -->
Weight 3: ConfigMap/Secret (prerequisites)
Weight 5: ServiceAccount
Weight 10: Job (default, user can override via $command.weight)

<!-- Hook type aggregation: prerequisite resources need ALL hook types from deployment -->
Current code (WRONG): prerequisite ConfigMap/Secret uses `$hookType` (single type from inner loop)
Required: aggregate all hook types with `keys $deploy.hooks | sortAlpha | join ","` BEFORE the range loop
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix all default -> hasKey in cronjob.yaml + tests</name>
  <files>charts/global-chart/templates/cronjob.yaml, charts/global-chart/tests/cronjob_test.yaml</files>
  <read_first>
    - charts/global-chart/templates/cronjob.yaml (full file — see current default usages)
    - charts/global-chart/tests/cronjob_test.yaml (existing tests to preserve)
    - .planning/phases/01-template-logic-audit-bug-fixes/01-RESEARCH.md (exact fix patterns)
    - .planning/codebase/CONVENTIONS.md (hasKey + ternary pattern reference)
  </read_first>
  <behavior>
    - Test: root-level CronJob with successfulJobsHistoryLimit: 0 renders spec.successfulJobsHistoryLimit = 0 (not 2)
    - Test: root-level CronJob with failedJobsHistoryLimit: 0 renders spec.failedJobsHistoryLimit = 0 (not 2)
    - Test: deployment-level CronJob with successfulJobsHistoryLimit: 0 renders spec.successfulJobsHistoryLimit = 0 (not 2)
    - Test: deployment-level CronJob with failedJobsHistoryLimit: 0 renders spec.failedJobsHistoryLimit = 0 (not 2)
    - Test: root-level CronJob with automountServiceAccountToken: false on SA renders false (not true)
    - Test: root-level CronJob without successfulJobsHistoryLimit still gets default 2
    - Test: root-level CronJob without concurrencyPolicy still gets default "Forbid"
    - Test: root-level CronJob without restartPolicy still gets default "Never"
  </behavior>
  <action>
    **Step 1: Write new test cases in cronjob_test.yaml** (RED phase)

    Add a new test group `# ====== falsy masking fixes ======` at the end of the file with these test cases:

    ```yaml
    - it: should render successfulJobsHistoryLimit 0 when explicitly set on root-level cronjob
      set:
        cronJobs:
          cleanup:
            schedule: "0 4 * * *"
            image: busybox:1.36
            command: ["./cleanup.sh"]
            successfulJobsHistoryLimit: 0
      asserts:
        - equal:
            path: spec.successfulJobsHistoryLimit
            value: 0

    - it: should render failedJobsHistoryLimit 0 when explicitly set on root-level cronjob
      set:
        cronJobs:
          cleanup:
            schedule: "0 4 * * *"
            image: busybox:1.36
            command: ["./cleanup.sh"]
            failedJobsHistoryLimit: 0
      asserts:
        - equal:
            path: spec.failedJobsHistoryLimit
            value: 0

    - it: should render automountServiceAccountToken false on root-level cronjob SA
      set:
        cronJobs:
          cleanup:
            schedule: "0 4 * * *"
            image: busybox:1.36
            command: ["./cleanup.sh"]
            serviceAccount:
              create: true
            automountServiceAccountToken: false
      asserts:
        - equal:
            path: automountServiceAccountToken
            value: false
          documentIndex: 0

    - it: should render successfulJobsHistoryLimit 0 on deployment-level cronjob
      set:
        deployments:
          backend:
            image: myapp:v2
            cronJobs:
              cleanup:
                schedule: "0 4 * * *"
                command: ["./cleanup.sh"]
                successfulJobsHistoryLimit: 0
      asserts:
        - equal:
            path: spec.successfulJobsHistoryLimit
            value: 0

    - it: should render failedJobsHistoryLimit 0 on deployment-level cronjob
      set:
        deployments:
          backend:
            image: myapp:v2
            cronJobs:
              cleanup:
                schedule: "0 4 * * *"
                command: ["./cleanup.sh"]
                failedJobsHistoryLimit: 0
      asserts:
        - equal:
            path: spec.failedJobsHistoryLimit
            value: 0
    ```

    **Step 2: Run tests to confirm they fail** (RED)

    Run `make unit-test` — the new tests MUST fail with value mismatch (expected 0, got 2 / expected false, got true).

    **Step 3: Fix cronjob.yaml** (GREEN)

    Convert ALL scalar `default` usages in cronjob.yaml. There are 10 locations across PART 1 (root-level) and PART 2 (deployment-level):

    **PART 1 (root-level) fixes:**
    - Line 33: `default $jobFullname $job.serviceAccountName` -> `ternary $job.serviceAccountName $jobFullname (hasKey $job "serviceAccountName")`
    - Line 47: `default true $job.automountServiceAccountToken` -> `ternary $job.automountServiceAccountToken true (hasKey $job "automountServiceAccountToken")`
    - Line 62: `default "Forbid" $job.concurrencyPolicy` -> `ternary $job.concurrencyPolicy "Forbid" (hasKey $job "concurrencyPolicy")`
    - Line 63: `default 2 $job.successfulJobsHistoryLimit` -> `ternary $job.successfulJobsHistoryLimit 2 (hasKey $job "successfulJobsHistoryLimit")`
    - Line 64: `default 2 $job.failedJobsHistoryLimit` -> `ternary $job.failedJobsHistoryLimit 2 (hasKey $job "failedJobsHistoryLimit")`
    - Line 152: `default "Never" $job.restartPolicy` -> `ternary $job.restartPolicy "Never" (hasKey $job "restartPolicy")`

    **PART 2 (deployment-level) fixes:**
    - Line 205: `default $jobFullname $jobSAMap.name` -> `ternary $jobSAMap.name $jobFullname (hasKey $jobSAMap "name")`
    - Line 257: `default "Forbid" $job.concurrencyPolicy` -> `ternary $job.concurrencyPolicy "Forbid" (hasKey $job "concurrencyPolicy")`
    - Line 258: `default 2 $job.successfulJobsHistoryLimit` -> `ternary $job.successfulJobsHistoryLimit 2 (hasKey $job "successfulJobsHistoryLimit")`
    - Line 259: `default 2 $job.failedJobsHistoryLimit` -> `ternary $job.failedJobsHistoryLimit 2 (hasKey $job "failedJobsHistoryLimit")`
    - Line 400: `default "Never" $job.restartPolicy` -> `ternary $job.restartPolicy "Never" (hasKey $job "restartPolicy")`

    For the `| quote` suffix conversions, keep the pipe: `{{ ternary $job.concurrencyPolicy "Forbid" (hasKey $job "concurrencyPolicy") | quote }}`

    **Do NOT convert** `default (dict)` or `default (list)` patterns — those are nil-guards, not falsy-masking.

    **Step 4: Run tests to confirm all pass** (GREEN)

    Run `make unit-test` — all existing tests plus the new ones MUST pass. If any existing test fails, the `hasKey` conversion changed behavior — fix it.
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test</automated>
  </verify>
  <acceptance_criteria>
    - cronjob.yaml contains zero occurrences of `default true`, `default 2`, `default "Forbid"`, `default "Never"` for scalar values (verify: `grep -E 'default (true|2|"Forbid"|"Never")' charts/global-chart/templates/cronjob.yaml` returns empty)
    - cronjob.yaml line 47 area contains `ternary $job.automountServiceAccountToken true (hasKey $job "automountServiceAccountToken")`
    - cronjob.yaml line 63 area contains `ternary $job.successfulJobsHistoryLimit 2 (hasKey $job "successfulJobsHistoryLimit")`
    - cronjob_test.yaml contains test `should render successfulJobsHistoryLimit 0 when explicitly set on root-level cronjob`
    - cronjob_test.yaml contains test `should render automountServiceAccountToken false on root-level cronjob SA`
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>All scalar default usages in cronjob.yaml converted to hasKey + ternary. Zero-value and false-value tests pass. All existing tests unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix all default -> hasKey in hook.yaml + fix prerequisite weights + fix hook type aggregation + tests</name>
  <files>charts/global-chart/templates/hook.yaml, charts/global-chart/tests/hook_test.yaml</files>
  <read_first>
    - charts/global-chart/templates/hook.yaml (full file — see current default usages, prerequisite weight lines, and hook type annotation on prerequisite ConfigMap/Secret at lines 282-327)
    - charts/global-chart/tests/hook_test.yaml (existing tests to preserve)
    - .planning/phases/01-template-logic-audit-bug-fixes/01-RESEARCH.md (weight hierarchy: 3/5/10)
    - .planning/phases/01-template-logic-audit-bug-fixes/01-CONTEXT.md (hook prerequisite decisions, especially "Hook types: aggregate all hook types from the deployment")
  </read_first>
  <behavior>
    - Test: root-level hook SA weight defaults to "5" when no weight override
    - Test: root-level hook Job weight defaults to "10" when no weight override
    - Test: deployment-level hook prerequisite ConfigMap gets weight "3"
    - Test: deployment-level hook prerequisite Secret gets weight "3"
    - Test: deployment-level hook SA gets weight "5"
    - Test: deployment-level hook Job gets weight "10"
    - Test: hook deletePolicy defaults to "before-hook-creation" when not set
    - Test: hook restartPolicy defaults to "Never" when not set
    - Test: when deployment has both pre-install and pre-upgrade hooks, prerequisite ConfigMap annotation `helm.sh/hook` contains "pre-install,pre-upgrade" (aggregated, not single type)
  </behavior>
  <action>
    **Step 1: Write new test cases in hook_test.yaml** (RED phase)

    Add a new test group `# ====== hook weight ordering ======` with tests for the weight hierarchy. For deployment-level hooks that generate ConfigMap + Secret + SA + Job (4 documents), verify each document's `helm.sh/hook-weight` annotation:

    ```yaml
    - it: should set weight 3 on prerequisite ConfigMap for deployment-level hook
      set:
        deployments:
          backend:
            image: myapp:v2
            configMap:
              DB_HOST: postgres
            hooks:
              pre-upgrade:
                migrate:
                  command: ["./migrate.sh"]
      asserts:
        - isKind:
            of: ConfigMap
          documentIndex: 0
        - equal:
            path: metadata.annotations["helm.sh/hook-weight"]
            value: "3"
          documentIndex: 0

    - it: should set weight 3 on prerequisite Secret for deployment-level hook
      set:
        deployments:
          backend:
            image: myapp:v2
            secret:
              DB_PASSWORD: secret123
            hooks:
              pre-upgrade:
                migrate:
                  command: ["./migrate.sh"]
      asserts:
        - isKind:
            of: Secret
          documentIndex: 0
        - equal:
            path: metadata.annotations["helm.sh/hook-weight"]
            value: "3"
          documentIndex: 0

    - it: should set weight 10 on Job for deployment-level hook
      set:
        deployments:
          backend:
            image: myapp:v2
            hooks:
              pre-upgrade:
                migrate:
                  command: ["./migrate.sh"]
      asserts:
        - isKind:
            of: Job
          documentIndex: 1
        - equal:
            path: metadata.annotations["helm.sh/hook-weight"]
            value: "10"
          documentIndex: 1
    ```

    Add a test group `# ====== hook type aggregation on prerequisites ======`:

    ```yaml
    - it: should aggregate all hook types on prerequisite ConfigMap when deployment has multiple hook types
      set:
        deployments:
          backend:
            image: myapp:v2
            configMap:
              DB_HOST: postgres
            hooks:
              pre-install:
                setup:
                  command: ["./setup.sh"]
              pre-upgrade:
                migrate:
                  command: ["./migrate.sh"]
      asserts:
        - isKind:
            of: ConfigMap
          documentIndex: 0
        - equal:
            path: metadata.annotations["helm.sh/hook"]
            value: "pre-install,pre-upgrade"
          documentIndex: 0
    ```

    Note: The prerequisite ConfigMap/Secret should be rendered ONCE per deployment (not per hook command), with ALL hook types aggregated. Currently the code renders them inside `range $hookType` / `range $name` which duplicates them. The restructuring in Step 3 addresses this.

    Also add a test group `# ====== hasKey default conversions ======` with:

    ```yaml
    - it: should render restartPolicy Never by default on root-level hook
      set:
        hooks:
          post-install:
            setup:
              image: busybox:1.36
              command: ["./setup.sh"]
      asserts:
        - equal:
            path: spec.template.spec.restartPolicy
            value: "Never"
          documentIndex: 1

    - it: should render deletePolicy before-hook-creation by default on root-level hook
      set:
        hooks:
          post-install:
            setup:
              image: busybox:1.36
              command: ["./setup.sh"]
      asserts:
        - equal:
            path: metadata.annotations["helm.sh/hook-delete-policy"]
            value: "before-hook-creation"
          documentIndex: 1
    ```

    **Step 2: Run tests** (RED) — new weight tests MUST fail (current weight is "5" for ConfigMap/Secret, not "3"). Hook type aggregation test MUST fail (current code uses single `$hookType`).

    **Step 3: Fix hook.yaml**

    **Hook type aggregation fix (per CONTEXT.md decision):**

    The prerequisite ConfigMap and Secret must be rendered ONCE per deployment (not per hook command), with `helm.sh/hook` annotation containing ALL hook types aggregated. Currently the code at lines 282-327 renders inside the `range $hookType` / `range $name` loop, producing duplicates.

    Restructure PART 2 of hook.yaml:
    1. BEFORE the `range $hookType` loop (but after the `$deploy.hooks` check), compute the aggregated hook types: `$allHookTypes := keys $deploy.hooks | sortAlpha | join ","`.
    2. Move the prerequisite ConfigMap block (lines 282-303) and Secret block (lines 305-327) OUTSIDE the `range $hookType` / `range $name` loop — render them once per deployment, right after computing `$allHookTypes`.
    3. Use `$allHookTypes` for their `helm.sh/hook` annotation instead of `$hookType`.
    4. Name them `{deploymentFullname}-hook-config` and `{deploymentFullname}-hook-secret` (using `$deployFullname` computed from `deploymentFullname` helper, not `$hookFullname` which includes hookType/name).
    5. Use hardcoded weight `"3"` and `"before-hook-creation"` delete-policy on these prerequisites (not `$command.weight`).

    The SA and Job blocks remain inside the inner loop (per hook command) as they are today.

    **Weight fixes (BUG-05):**
    - Prerequisite ConfigMap/Secret (now outside loop): hardcode weight `"3"`
    - Lines 265-266 (deployment-level hook SA): keep weight as `"5"`, convert deletePolicy `default` -> `ternary`
    - Lines 338-339 (deployment-level hook Job): `ternary $command.weight "10" (hasKey $command "weight")` — user can override Job weight

    **hasKey conversion (BUG-03):**

    PART 1 (root-level hooks):
    - Line 33: `default $hookFullname $commandSAExplicitName` -> `ternary $commandSAExplicitName $hookFullname (ne $commandSAExplicitName "")` (coalesce already handles nil, but make it explicit)
    - Line 65: `default "5" $command.weight` -> `ternary $command.weight "5" (hasKey $command "weight")`
    - Line 66: `default "before-hook-creation" $command.deletePolicy` -> `ternary $command.deletePolicy "before-hook-creation" (hasKey $command "deletePolicy")`
    - Line 81: `default "10" $command.weight` -> `ternary $command.weight "10" (hasKey $command "weight")`
    - Line 82: `default "before-hook-creation" $command.deletePolicy` -> same ternary pattern
    - Line 113: `default "Never" $command.restartPolicy` -> `ternary $command.restartPolicy "Never" (hasKey $command "restartPolicy")`

    PART 2 (deployment-level hooks):
    - Line 232: `default $hookFullname $commandSAMap.name` -> `ternary $commandSAMap.name $hookFullname (hasKey $commandSAMap "name")`
    - Line 265: SA hook-weight — keep "5", convert deletePolicy `default` -> `ternary`
    - Line 266: SA delete-policy — `ternary $command.deletePolicy "before-hook-creation" (hasKey $command "deletePolicy")`
    - Line 338: Job hook-weight — `ternary $command.weight "10" (hasKey $command "weight")`
    - Line 339: Job delete-policy — same ternary pattern
    - Line 374: `default "Never" $command.restartPolicy` -> `ternary $command.restartPolicy "Never" (hasKey $command "restartPolicy")`

    **Do NOT convert** `default (dict)` patterns (e.g., line 196 `default (dict) $deploy.serviceAccount`) — those are nil-guards.

    **Step 4: Run tests** (GREEN) — all tests MUST pass. Verify that:
    - Existing tests referencing prerequisite ConfigMap/Secret document indices may need adjustment due to the restructured rendering order (prerequisites now render before per-command SA/Job documents).
    - The aggregation test passes with "pre-install,pre-upgrade" (sorted alphabetically).
  </action>
  <verify>
    <automated>cd /Users/filippomerante/project/github/global-chart && make unit-test</automated>
  </verify>
  <acceptance_criteria>
    - hook.yaml prerequisite ConfigMap/Secret blocks are rendered ONCE per deployment (outside the hookType/command loop), not per hook command
    - hook.yaml prerequisite ConfigMap/Secret `helm.sh/hook` annotation uses aggregated hook types (e.g., `keys $deploy.hooks | sortAlpha | join ","`)
    - hook.yaml prerequisite ConfigMap/Secret use hardcoded weight `"3"` (verify: `grep 'hook-weight.*"3"' charts/global-chart/templates/hook.yaml` returns matches)
    - hook.yaml contains zero occurrences of `default "Never"`, `default "before-hook-creation"` for scalar fields
    - hook_test.yaml contains test `should set weight 3 on prerequisite ConfigMap for deployment-level hook`
    - hook_test.yaml contains test `should aggregate all hook types on prerequisite ConfigMap when deployment has multiple hook types`
    - When a deployment has pre-install and pre-upgrade hooks, prerequisite ConfigMap annotation contains both types
    - `make unit-test` exits 0
  </acceptance_criteria>
  <done>All scalar default usages in hook.yaml converted to hasKey + ternary. Hook prerequisite ConfigMap/Secret use weight "3", SA uses "5", Job uses "10". Prerequisite resources render once per deployment with aggregated hook types. All tests pass.</done>
</task>

</tasks>

<verification>
Run `make all` (lint + unit-test + generate-templates) to verify full chart integrity after both tasks.

Post-completion checks:
- `grep -cE 'default (true|false|2|"Forbid"|"Never"|"5"|"10"|"before-hook-creation")' charts/global-chart/templates/cronjob.yaml` should return 0
- `grep -cE 'default "(Never|before-hook-creation)"' charts/global-chart/templates/hook.yaml` should return 0
- `grep 'hook-weight.*"3"' charts/global-chart/templates/hook.yaml` should return matches for ConfigMap + Secret prerequisites
- `grep 'sortAlpha.*join' charts/global-chart/templates/hook.yaml` should return a match (hook type aggregation)
</verification>

<success_criteria>
- Zero falsy masking in cronjob.yaml and hook.yaml (all scalar defaults converted)
- Hook prerequisites use correct weight ordering (3 < 5 < 10)
- Hook prerequisite ConfigMap/Secret aggregate all hook types from the deployment
- All existing 220+ tests pass unchanged (or updated for new document ordering)
- New tests verify zero-value, false-value, weight ordering, and hook type aggregation behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-logic-audit-bug-fixes/01-01-SUMMARY.md`
</output>
